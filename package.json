{
  "name": "meloasr",
  "productName": "MeloASR",
  "version": "0.1.9",
  "private": true,
  "author": "MeloASR contributors",
  "description": "基于网页语音识别后端和 Fcitx5 的 Linux 实时语音输入工具",
  "main": "dist/main/index.js",
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "build": "npm run clean && tsc && esbuild src/preload/backend.ts --bundle --platform=node --format=cjs --external:electron --outfile=dist/preload/backend.js && node scripts/copy-static.mjs",
    "typecheck": "tsc --noEmit",
    "start": "npm run build && electron --ozone-platform=x11 .",
    "build:fcitx5": "cmake -S linux/fcitx5 -B linux/fcitx5/build -DCMAKE_BUILD_TYPE=Release && cmake --build linux/fcitx5/build --parallel",
    "build:linux:dir": "npm run build && electron-builder --config packaging/electron-builder.yml --linux dir",
    "package:deb": "npm run build:linux:dir && packaging/deb/build-deb.sh",
    "package:rpm": "npm run build:linux:dir && packaging/rpm/build-rpm.sh",
    "test": "npm run typecheck && tsc -p tsconfig.test.json && node --test dist-test/test/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "electron": "42.3.0",
    "electron-builder": "^26.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.9.0"
  }
}
