# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的格式。

## [0.1.17] - 2026-09-01

### 变更

- 更新应用图标，并将托盘图标统一为“已就绪”和“未就绪”两种状态。

## [0.1.16] - 2026-08-26

### 修复

- 项目依赖管理、构建脚本、Arch 打包和 Release 工作流统一迁移到 pnpm，避免 npm 将第三方镜像地址识别为禁用的 remote 包。
- 使用不包含 registry 下载地址的 `pnpm-lock.yaml` 替代 `package-lock.json`，使锁文件不再固化生成环境的 npm 镜像。

## [0.1.15] - 2026-08-26

### 修复

- Arch PKGBUILD 改为直接下载 GitHub Release 标签对应的源码归档，不再要求发布者或用户手动放置源码包。
- 修正 GitHub 标签归档的解压目录处理，使 `makepkg` 可以直接完成构建、测试和打包。
- 远程 Release 现在附带当前版本的 `PKGBUILD`，Arch 用户无需再从源码树中单独提取打包文件。

## [0.1.14] - 2026-08-26

### 性能

- 语音会话开始时缓存网页编辑器根元素，后续文本更新直接序列化该元素，避免每次更新重复查询 DOM、计算样式和读取布局。
- 复用会话启动时的初始文本序列化结果，减少一次重复的完整编辑器遍历。

## [0.1.13] - 2026-08-24

### 变更

- 设置页改为按需创建，关闭窗口后立即销毁其 renderer 以降低常驻内存。
- 缩小设置页默认与最小窗口尺寸，使其匹配当前功能密度。

## [0.1.12] - 2026-08-23

### 变更

- 全平台启动时强制禁用 Electron GPU 硬件加速，降低后台网页 renderer 的无效显存与内存占用。

## [0.1.11] - 2026-08-23

### 变更

- 切换后端时销毁非当前后端的网页窗口，保留登录会话但不再保活其 renderer。
- 移除悬浮球点击录音和常驻显示功能，悬浮球仅用于显示语音会话状态。
- 缩短正常完成后的绿色状态显示时间。

## [0.1.10] - 2026-08-23

### 修复

- 修复 Nix 构建跳过 npm 依赖配置，导致 `tsc` 不可用的问题。

## [0.1.9] - 2026-08-23

### 修复

- 修复 arm64 deb/rpm 构建使用错误 Electron 解包目录的问题。
- 修复 RPM 打包时 Fcitx5 addon 文件路径校验失败的问题。
- 修复 Nix 构建错误对项目根目录执行 CMake 配置的问题。

## [0.1.8] - 2026-08-23

### 修复

- 兼容 Ubuntu 24.04 提供的 Fcitx5 5.1.7 头文件布局，使 Fcitx5 addon 可以在发行构建中编译。

## [0.1.7] - 2026-08-23

### 新增

- 提供基于 Fcitx5 的 Linux 实时语音输入流程、悬浮球、托盘与设置页。
- 支持千问和元宝网页后端，以及 deb、rpm、Arch 与 Nix 分发配置。

### 修复

- 改进悬浮球、托盘状态和网页后端就绪状态的交互。
